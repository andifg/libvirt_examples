# Set LIBVIRT_* in environment (see .envrc for this directory).
---
ansible_host: "{{ lookup('env', 'LIBVIRT_ANSIBLE_HOST') }}"
ansible_user: "{{ lookup('env', 'LIBVIRT_ANSIBLE_USER') }}"
ansible_ssh_private_key_file: "{{ lookup('env', 'LIBVIRT_ANSIBLE_SSH_KEY') }}"
